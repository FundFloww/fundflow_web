import { IdeasFilterPipe } from './ideas-filter.pipe';

describe('IdeasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdeasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
