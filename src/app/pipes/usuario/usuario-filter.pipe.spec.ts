import { UsuarioFilterPipe } from './usuario-filter.pipe';

describe('UsuarioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
